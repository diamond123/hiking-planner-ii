{
  "name": "hiking-planner-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5500",
    "build": "vite build && vite preview --host 127.0.0.1 --port 5500"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
